#pragma once

class ServerManager
{
    private:
        
    public:
        ServerManager();
        ServerManager(const ServerManager &rhs);
        ServerManager &operator =(const ServerManager &rhs);
        ~ServerManager();

};

